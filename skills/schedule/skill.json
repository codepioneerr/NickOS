{
  "name": "schedule",
  "version": "1.0.0",
  "description": "View today's nudge schedule, trigger an instant status check",
  "author": "nickos",
  "commands": [
    {
      "command": "schedule",
      "description": "Today's remaining nudge times",
      "handler": "schedule_handler"
    },
    {
      "command": "nudge",
      "description": "Get an immediate status check",
      "handler": "nudge_handler"
    }
  ]
}
